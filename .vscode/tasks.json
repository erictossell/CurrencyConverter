{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet build -o ${workspaceRoot}/app/pages",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$msCompile"
        }
    ]
}
